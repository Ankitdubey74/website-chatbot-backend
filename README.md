This project is the backend for AI Aether’s official FAQ Chatbot, built using Flask, Groq LLaMA-3, and JSON-based FAQ retrieval.
The chatbot reads a structured FAQ file, analyzes user queries, and returns clear, professional replies.

✨ Features
📚 Loads FAQ automatically from JSON
🤖 Uses Groq LLaMA-3–70B for intellgent answers
🔄 Clean and structured responses (bullet poins, line breaks, etc.)
🔐 API key secured using environment variables
🌐 CORS enabled for frontend integration
💬 /chat endpoint for chatbot messages
🖥️ /chat_interface serves UI (chat.html)
🟢 Render-ready configuration
